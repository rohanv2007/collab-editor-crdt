# Optional production WebSocket endpoint for the static client.
# Vercel cannot host the raw ws server directly, so point the deployed client
# at a persistent WebSocket host, for example:
# COLLAB_WS_URL=wss://your-collab-server.example.com
